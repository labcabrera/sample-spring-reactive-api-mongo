spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/samples-reactive-api

logging:
  level:
    root: debug
    org.lab: debug
    org.mongodb.driver: info

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
