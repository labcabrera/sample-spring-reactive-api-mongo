spring:
  data:
    mongodb:
      host: localhost

logging:
  level:
    root: debug
    org.lab: debug
    org.mongodb.driver: info

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
